# Cloudflare Pages (site estático na raiz do repositório).
# Deploy manual: na raiz do projeto → npm run pages:deploy
# Ligação Git no dashboard: Build command = npm run build, Output = /
name = "controle-cartao"
pages_build_output_dir = "."
compatibility_date = "2024-12-01"
