# Cloudflare Pages (Git)
# — No painel do projeto: Build output directory = / (raiz do repositório).
# — Variáveis de build: CF_API_BASE, CF_FIREBASE_API_KEY, CF_FIREBASE_AUTH_DOMAIN, CF_FIREBASE_PROJECT_ID
#   (Settings → Environment variables). Sem elas, api-config.js fica vazio e não há login/sync.
name = "controle-cartao"
compatibility_date = "2024-12-01"
pages_build_output_dir = "."
