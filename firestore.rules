rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Apenas o backend (Worker com conta de serviço) acede ao Firestore.
    // Clientes não devem usar o SDK Firestore neste projeto.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
