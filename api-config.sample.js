/**
 * Copie para api-config.js (não versionado) ou use o build no Pages com variáveis de ambiente.
 * Worker: URL pública do Cloudflare Worker.
 * Firebase: Console > Configurações do projeto > Suas apps > SDK (config).
 */
window.CONTROLE_API = {
  base: "https://SEU-WORKER.workers.dev",
};

window.FIREBASE_WEB = {
  apiKey: "AIza...",
  authDomain: "seu-projeto.firebaseapp.com",
  projectId: "seu-projeto-firebase",
};
