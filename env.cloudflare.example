# Copiar valores para o Cloudflare Pages → Settings → Environment variables (Production)
# e para o teu ambiente local ao correr: npm run build

CF_API_BASE=https://controle-cartao-api.SEU_SUBDOMINIO.workers.dev
CF_FIREBASE_API_KEY=
CF_FIREBASE_AUTH_DOMAIN=
CF_FIREBASE_PROJECT_ID=

# Firebase Console → Authentication → Settings → Authorized domains → adicionar:
#   controle-cartao.pages.dev
